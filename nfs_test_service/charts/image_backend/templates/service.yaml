# argocd_deploy_repo/nfs_test_service/charts/image_backend/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: svc-image-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: image-backend